<footer class="mt-12">
    <div class="container mx-auto px-4 sm:px-6 lg:px-8 py-6 flex flex-col md:flex-row items-center justify-between">
        <p class="text-gray-600 text-sm">&copy; {{ "now"|date("Y") }} Ravitar. Alle Rechte vorbehalten.</p>
        <div class="flex space-x-4 mt-4 md:mt-0">
            <a href="{{ path('datenschutz') }}" class="text-gray-600 hover:text-indigo-600 text-sm">Datenschutz</a>
            <a href="{{ path('nutzungsbedingungen') }}" class="text-gray-600 hover:text-indigo-600 text-sm">AGB</a>
            <a href="{{ path('impressum') }}" class="text-gray-600 hover:text-indigo-600 text-sm">Impressum</a>
        </div>
    </div>
</footer>
